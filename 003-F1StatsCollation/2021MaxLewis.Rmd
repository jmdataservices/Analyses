---
title: "2021 - Max vs Lewis"
author: "Jamie McLaughlin"
date: "2023-03-12"
output:
        html_document:
                css: "styles.css"
---

![F1 Flyover, Photo by Abed Ismail on Unsplash](src/f1flyover.jpg)

```{r setup, include = F}

knitr::opts_chunk$set(
        echo = F,
        message = F,
        warning = F,
        scipen = 999,
        fig.height = 12,
        fig.width = 14,
        fig.align = "center"
)

#############################################################################
# Ingest
#############################################################################

rm(list = ls())
library(mossR)
library(gridExtra)
library(cowplot)

lap_times <- read.csv("data/lap_times.csv")
pit_stops <- read.csv("data/pit_stops.csv")
drivers <- read.csv("data/drivers.csv")
races <- read.csv("data/races.csv")
circuits <- read.csv("data/circuits.csv")
results <- read.csv("data/results.csv")
constructors <- read.csv("data/constructors.csv")
status <- read.csv("data/status.csv")

laptimes <- lap_times %>% 
        select(
                driverId,
                RaceID = raceId,
                Lap = lap,
                CurrentPosition = position,
                Laptime = milliseconds
        ) %>% 
        left_join(
                by = "driverId",
                drivers %>% 
                        mutate(
                                DriverName = paste(sep = " ", forename, surname)
                        ) %>% 
                        select(
                                driverId,
                                DriverName
                        )
        ) %>% 
        left_join(
                by = c("RaceID" = "raceId"),
                races %>% 
                        select(
                                raceId,
                                circuitId,
                                Year = year,
                                Round = round,
                                RaceDate = date,
                                RaceName = name
                        )
        ) %>% 
        left_join(
                by = "circuitId",
                circuits %>% 
                        select(
                                circuitId,
                                CircuitRef = circuitRef,
                                CircuitName = name,
                                Lat = lat,
                                Lng = lng,
                                Alt = alt,
                                Location = location,
                                Country = country
                        )
        ) %>% 
        left_join(
                by = c("RaceID" = "raceId", "driverId"),
                results %>% 
                        select(
                                raceId,
                                driverId,
                                constructorId,
                                GridSpot = grid,
                                statusId
                        ) %>% 
                        left_join(
                                by = "statusId",
                                status %>% 
                                        select(
                                                statusId,
                                                Status = status
                                        )
                        )
        ) %>% 
        left_join(
                by = c("constructorId"),
                constructors %>% 
                        select(
                                constructorId,
                                TeamName = name
                        )
        ) %>% 
        select(
                -driverId,
                -constructorId,
                -circuitId,
                -statusId
        ) %>% 
        group_by(
                RaceID
        ) %>% 
        mutate(
                RaceLaps = max(Lap, na.rm = T)
        ) %>% 
        group_by(
                RaceID,
                DriverName
        ) %>% 
        mutate(
                RaceTime = cumsum(Laptime)
        )

#############################################################################
# Max vs Lewis 2021
#############################################################################

laptimes_lm2021 <- laptimes %>% 
        filter(
                Year == 2021,
                DriverName %in% c("Lewis Hamilton", "Max Verstappen")
        ) %>% as.data.frame()

```

## Intro

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

```{r intro}

laptimes_lm2021_diffs <- laptimes_lm2021 %>% 
        filter(
                !(Round %in% c(10, 12))
        ) %>% 
        pivot_wider(
                id_cols = c(RaceID, Round, RaceName, Lap, RaceLaps),
                names_from = DriverName,
                values_from = RaceTime
        ) %>% 
        mutate(
                TimeDiff = (`Lewis Hamilton` - `Max Verstappen`) / 1000
        ) %>% 
        group_by(
                Round
        ) %>% 
        mutate(
                MaxDiff = max(abs(TimeDiff), na.rm = T)
        )

laptimes_lm2021_diffs %>% 
        mutate(
                RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
        ) %>% 
        ggplot() +
        geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
        geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
        #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
        #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
        geom_text(aes(x = 1, y = -50, label = "Lewis"), color = "seagreen", hjust = 0) +
        geom_text(aes(x = 1, y = 50, label = "Max"), color = "navy", hjust = 0) +
        theme_msc() +
        theme(
                axis.text.x = element_blank(),
                axis.text.y = element_text(size = 9),
                strip.text = element_text(size = 10),
                strip.background = element_rect(color = "grey50")
        ) +
        labs(
                title = "Race Length Time Differences",
                subtitle = "Race 10 saw Max crash out on the opening lap at Silverstone,\nwhile race 12 at Spa was cancelled.",
                x = "Time",
                y = "Lead"
        ) +
        scale_y_continuous(
                limits = c(-80, 80),
                labels = function(x) {paste0(abs(x), " secs")}
        ) +
        facet_wrap(
                RoundRace ~ .
        )

```

## Round 1

```{r round1, fig.height = 20}

plt_multi <- list(
        laptimes_lm2021 %>% 
                filter(
                        Round == 1
                ) %>% 
                ggplot() +
                geom_line(aes(x = Lap, y = Laptime, group = DriverName, color = DriverName), size = 1) +
                geom_point(aes(x = Lap, y = Laptime, color = DriverName), size = 3) +
                theme_msc() +
                theme(
                        panel.grid.major.y = element_line(linewidth = 0.5, linetype = "dashed", color = "grey80"),
                        legend.position = "top",
                        panel.background = element_rect(fill = "#000000", color = "#000000"),
                        plot.background = element_rect(fill = "#000000", color = "#000000"),
                        plot.title = element_text(color = "#ffffff"),
                        plot.subtitle = element_text(color = "#ffffff"),
                        axis.text = element_text(color = "#ffffff"),
                        axis.text.x = element_blank(),
                        strip.text = element_text(color = "#ffffff")
                ) +
                labs(
                        title = "Lap Times During Round 1",
                        subtitle = "Bahrain Grand Prix",
                        x = "",
                        y = "Lap",
                        color = ""
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(floor(x / 60000), ":", str_pad((x / 1000) %% 60, 2, pad = "0"))},
                        breaks = c(seq(0, 175000, 5000))
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis Hamilton" = "seagreen",
                                "Max Verstappen" = "navy"
                        )
                ),# +
                #coord_cartesian(
                #        ylim = c(92500, 100000)
                #),
        laptimes_lm2021_diffs %>% 
                filter(
                        Round == 1
                ) %>% 
                mutate(
                        RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
                ) %>% 
                ggplot() +
                geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
                geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
                #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
                #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
                geom_text(aes(x = 1, y = -MaxDiff, label = "Lewis"), color = "seagreen", hjust = 0, size = 5) +
                geom_text(aes(x = 1, y = MaxDiff, label = "Max"), color = "navy", hjust = 0, size = 5) +
                geom_text(aes(x = Lap, y = ifelse(Lap == RaceLaps, TimeDiff + ifelse(TimeDiff < 0, -3, 3), NA), label = TimeDiff, color = ifelse(TimeDiff < 0, "Lewis", "Max")), size = 5) +
                theme_msc() +
                theme(
                        panel.background = element_rect(fill = "#000000", color = "#000000"),
                        plot.background = element_rect(fill = "#000000", color = "#000000"),
                        plot.title = element_text(color = "#ffffff"),
                        plot.subtitle = element_text(color = "#ffffff"),
                        axis.text = element_text(color = "#ffffff", size = 11),
                        axis.text.x = element_blank(),
                        strip.text = element_text(color = "#ffffff", size = 10),
                        strip.background = element_rect(color = "grey50")
                ) +
                labs(
                        x = "Lap Number",
                        y = "Lead"
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(abs(x), " secs")}
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis" = "seagreen",
                                "Max" = "navy"
                        )
                )
)

plot_grid(
        plt_multi[[1]],
        plt_multi[[2]],
        align = "v",
        nrow = 2,
        rel_heights = c(3/4, 1/4)
)

```

## Round 2

```{r round2, fig.height = 20}

plt_multi <- list(
        laptimes_lm2021 %>% 
                filter(
                        Round == 2
                ) %>% 
                ggplot() +
                geom_line(aes(x = Lap, y = Laptime, group = DriverName, color = DriverName), size = 1) +
                geom_point(aes(x = Lap, y = Laptime, color = DriverName), size = 3) +
                theme_msc() +
                theme(
                        panel.grid.major.y = element_line(linewidth = 0.5, linetype = "dashed", color = "grey80"),
                        axis.text.x = element_blank(),
                        legend.position = "top"
                ) +
                labs(
                        title = "Lap Times During Round 2",
                        subtitle = "Emilia Romagna Grand Prix",
                        x = "",
                        y = "Lap",
                        color = ""
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(floor(x / 60000), ":", str_pad((x / 1000) %% 60, 2, pad = "0"))},
                        breaks = c(seq(0, 175000, 5000))
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis Hamilton" = "seagreen",
                                "Max Verstappen" = "navy"
                        )
                ) +
                coord_cartesian(
                        ylim = c(75000, 150000)
                ),
        laptimes_lm2021_diffs %>% 
                filter(
                        Round == 2
                ) %>% 
                mutate(
                        RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
                ) %>% 
                ggplot() +
                geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
                geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
                #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
                #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
                geom_text(aes(x = 1, y = -MaxDiff, label = "Lewis"), color = "seagreen", hjust = 0, size = 5) +
                geom_text(aes(x = 1, y = MaxDiff, label = "Max"), color = "navy", hjust = 0, size = 5) +
                geom_text(aes(x = Lap, y = ifelse(Lap == RaceLaps, TimeDiff + ifelse(TimeDiff < 0, -3, 3), NA), label = TimeDiff, color = ifelse(TimeDiff < 0, "Lewis", "Max")), size = 5) +
                theme_msc() +
                theme(
                        axis.text = element_text(size = 11),
                        strip.text = element_text(size = 10),
                        strip.background = element_rect(color = "grey50")
                ) +
                labs(
                        x = "Lap Number",
                        y = "Lead"
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(abs(x), " secs")}
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis" = "seagreen",
                                "Max" = "navy"
                        )
                ) +
                coord_cartesian(
                        ylim = c(-30, 30)
                )
)

plot_grid(
        plt_multi[[1]],
        plt_multi[[2]],
        align = "v",
        nrow = 2,
        rel_heights = c(3/4, 1/4)
)

```

## Round 3

```{r round3}

plt_multi <- list(
        laptimes_lm2021 %>% 
                filter(
                        Round == 3
                ) %>% 
                ggplot() +
                geom_line(aes(x = Lap, y = Laptime, group = DriverName, color = DriverName), size = 1) +
                geom_point(aes(x = Lap, y = Laptime, color = DriverName), size = 3) +
                theme_msc() +
                theme(
                        panel.grid.major.y = element_line(linewidth = 0.5, linetype = "dashed", color = "grey80"),
                        axis.text.x = element_blank(),
                        legend.position = "top"
                ) +
                labs(
                        title = "Lap Times During Round 3",
                        subtitle = "Portuguese Grand Prix",
                        x = "",
                        y = "Lap",
                        color = ""
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(floor(x / 60000), ":", str_pad((x / 1000) %% 60, 2, pad = "0"))},
                        breaks = c(seq(0, 175000, 5000))
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis Hamilton" = "seagreen",
                                "Max Verstappen" = "navy"
                        )
                ),
        laptimes_lm2021_diffs %>% 
                filter(
                        Round == 3
                ) %>% 
                mutate(
                        RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
                ) %>% 
                ggplot() +
                geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
                geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
                #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
                #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
                geom_text(aes(x = 1, y = -MaxDiff, label = "Lewis"), color = "seagreen", hjust = 0, size = 5) +
                geom_text(aes(x = 1, y = MaxDiff, label = "Max"), color = "navy", hjust = 0, size = 5) +
                geom_text(aes(x = Lap, y = ifelse(Lap == RaceLaps, TimeDiff + ifelse(TimeDiff < 0, -3, 3), NA), label = TimeDiff, color = ifelse(TimeDiff < 0, "Lewis", "Max")), size = 5) +
                theme_msc() +
                theme(
                        axis.text = element_text(size = 11),
                        strip.text = element_text(size = 10),
                        strip.background = element_rect(color = "grey50")
                ) +
                labs(
                        x = "Lap Number",
                        y = "Lead"
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(abs(x), " secs")}
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis" = "seagreen",
                                "Max" = "navy"
                        )
                )
)

plot_grid(
        plt_multi[[1]],
        plt_multi[[2]],
        align = "v",
        nrow = 2,
        rel_heights = c(3/4, 1/4)
)

```

## Round 4

```{r round4}

plt_multi <- list(
        laptimes_lm2021 %>% 
                filter(
                        Round == 4
                ) %>% 
                ggplot() +
                geom_line(aes(x = Lap, y = Laptime, group = DriverName, color = DriverName), size = 1) +
                geom_point(aes(x = Lap, y = Laptime, color = DriverName), size = 3) +
                theme_msc() +
                theme(
                        panel.grid.major.y = element_line(linewidth = 0.5, linetype = "dashed", color = "grey80"),
                        axis.text.x = element_blank(),
                        legend.position = "top"
                ) +
                labs(
                        title = "Lap Times During Round 4",
                        subtitle = "Spanish Grand Prix",
                        x = "",
                        y = "Lap",
                        color = ""
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(floor(x / 60000), ":", str_pad((x / 1000) %% 60, 2, pad = "0"))},
                        breaks = c(seq(0, 175000, 5000))
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis Hamilton" = "seagreen",
                                "Max Verstappen" = "navy"
                        )
                ),
        laptimes_lm2021_diffs %>% 
                filter(
                        Round == 4
                ) %>% 
                mutate(
                        RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
                ) %>% 
                ggplot() +
                geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
                geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
                #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
                #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
                geom_text(aes(x = 1, y = -MaxDiff, label = "Lewis"), color = "seagreen", hjust = 0, size = 5) +
                geom_text(aes(x = 1, y = MaxDiff, label = "Max"), color = "navy", hjust = 0, size = 5) +
                geom_text(aes(x = Lap, y = ifelse(Lap == RaceLaps, TimeDiff + ifelse(TimeDiff < 0, -3, 3), NA), label = TimeDiff, color = ifelse(TimeDiff < 0, "Lewis", "Max")), size = 5) +
                theme_msc() +
                theme(
                        axis.text = element_text(size = 11),
                        strip.text = element_text(size = 10),
                        strip.background = element_rect(color = "grey50")
                ) +
                labs(
                        x = "Lap Number",
                        y = "Lead"
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(abs(x), " secs")}
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis" = "seagreen",
                                "Max" = "navy"
                        )
                )
)

plot_grid(
        plt_multi[[1]],
        plt_multi[[2]],
        align = "v",
        nrow = 2,
        rel_heights = c(3/4, 1/4)
)

```

## Round 5

```{r round5}

plt_multi <- list(
        laptimes_lm2021 %>% 
                filter(
                        Round == 5
                ) %>% 
                ggplot() +
                geom_line(aes(x = Lap, y = Laptime, group = DriverName, color = DriverName), size = 1) +
                geom_point(aes(x = Lap, y = Laptime, color = DriverName), size = 3) +
                theme_msc() +
                theme(
                        panel.grid.major.y = element_line(linewidth = 0.5, linetype = "dashed", color = "grey80"),
                        axis.text.x = element_blank(),
                        legend.position = "top"
                ) +
                labs(
                        title = "Lap Times During Round 5",
                        subtitle = "Monaco Grand Prix",
                        x = "",
                        y = "Lap",
                        color = ""
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(floor(x / 60000), ":", str_pad((x / 1000) %% 60, 2, pad = "0"))},
                        breaks = c(seq(0, 175000, 5000))
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis Hamilton" = "seagreen",
                                "Max Verstappen" = "navy"
                        )
                ),
        laptimes_lm2021_diffs %>% 
                filter(
                        Round == 5
                ) %>% 
                mutate(
                        RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
                ) %>% 
                ggplot() +
                geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
                geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
                #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
                #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
                geom_text(aes(x = 1, y = -MaxDiff, label = "Lewis"), color = "seagreen", hjust = 0, size = 5) +
                geom_text(aes(x = 1, y = MaxDiff, label = "Max"), color = "navy", hjust = 0, size = 5) +
                geom_text(aes(x = Lap, y = ifelse(Lap == RaceLaps, TimeDiff + ifelse(TimeDiff < 0, -3, 3), NA), label = TimeDiff, color = ifelse(TimeDiff < 0, "Lewis", "Max")), size = 5) +
                theme_msc() +
                theme(
                        axis.text = element_text(size = 11),
                        strip.text = element_text(size = 10),
                        strip.background = element_rect(color = "grey50")
                ) +
                labs(
                        x = "Lap Number",
                        y = "Lead"
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(abs(x), " secs")}
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis" = "seagreen",
                                "Max" = "navy"
                        )
                )
)

plot_grid(
        plt_multi[[1]],
        plt_multi[[2]],
        align = "v",
        nrow = 2,
        rel_heights = c(3/4, 1/4)
)

```

## Round 6

```{r round6}

plt_multi <- list(
        laptimes_lm2021 %>% 
                filter(
                        Round == 6
                ) %>% 
                ggplot() +
                geom_line(aes(x = Lap, y = Laptime, group = DriverName, color = DriverName), size = 1) +
                geom_point(aes(x = Lap, y = Laptime, color = DriverName), size = 3) +
                theme_msc() +
                theme(
                        panel.grid.major.y = element_line(linewidth = 0.5, linetype = "dashed", color = "grey80"),
                        axis.text.x = element_blank(),
                        legend.position = "top"
                ) +
                labs(
                        title = "Lap Times During Round 6",
                        subtitle = "Azerbaijan Grand Prix",
                        x = "",
                        y = "Lap",
                        color = ""
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(floor(x / 60000), ":", str_pad((x / 1000) %% 60, 2, pad = "0"))},
                        breaks = c(seq(0, 175000, 5000))
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis Hamilton" = "seagreen",
                                "Max Verstappen" = "navy"
                        )
                ) +
                coord_cartesian(
                        ylim = c(75000, 150000)
                ),
        laptimes_lm2021_diffs %>% 
                filter(
                        Round == 6
                ) %>% 
                mutate(
                        RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
                ) %>% 
                ggplot() +
                geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
                geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
                #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
                #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
                geom_text(aes(x = 1, y = -MaxDiff, label = "Lewis"), color = "seagreen", hjust = 0, size = 5) +
                geom_text(aes(x = 1, y = MaxDiff, label = "Max"), color = "navy", hjust = 0, size = 5) +
                geom_text(aes(x = Lap, y = ifelse(Lap == RaceLaps, TimeDiff + ifelse(TimeDiff < 0, -3, 3), NA), label = TimeDiff, color = ifelse(TimeDiff < 0, "Lewis", "Max")), size = 5) +
                theme_msc() +
                theme(
                        axis.text = element_text(size = 11),
                        strip.text = element_text(size = 10),
                        strip.background = element_rect(color = "grey50")
                ) +
                labs(
                        x = "Lap Number",
                        y = "Lead"
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(abs(x), " secs")}
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis" = "seagreen",
                                "Max" = "navy"
                        )
                )
)

plot_grid(
        plt_multi[[1]],
        plt_multi[[2]],
        align = "v",
        nrow = 2,
        rel_heights = c(3/4, 1/4)
)

```

## Round 7

```{r round7}

plt_multi <- list(
        laptimes_lm2021 %>% 
                filter(
                        Round == 7
                ) %>% 
                ggplot() +
                geom_line(aes(x = Lap, y = Laptime, group = DriverName, color = DriverName), size = 1) +
                geom_point(aes(x = Lap, y = Laptime, color = DriverName), size = 3) +
                theme_msc() +
                theme(
                        panel.grid.major.y = element_line(linewidth = 0.5, linetype = "dashed", color = "grey80"),
                        axis.text.x = element_blank(),
                        legend.position = "top"
                ) +
                labs(
                        title = "Lap Times During Round 7",
                        subtitle = "French Grand Prix",
                        x = "",
                        y = "Lap",
                        color = ""
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(floor(x / 60000), ":", str_pad((x / 1000) %% 60, 2, pad = "0"))},
                        breaks = c(seq(0, 175000, 5000))
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis Hamilton" = "seagreen",
                                "Max Verstappen" = "navy"
                        )
                ),
        laptimes_lm2021_diffs %>% 
                filter(
                        Round == 7
                ) %>% 
                mutate(
                        RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
                ) %>% 
                ggplot() +
                geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
                geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
                #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
                #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
                geom_text(aes(x = 1, y = -MaxDiff, label = "Lewis"), color = "seagreen", hjust = 0, size = 5) +
                geom_text(aes(x = 1, y = MaxDiff, label = "Max"), color = "navy", hjust = 0, size = 5) +
                geom_text(aes(x = Lap, y = ifelse(Lap == RaceLaps, TimeDiff + ifelse(TimeDiff < 0, -3, 3), NA), label = TimeDiff, color = ifelse(TimeDiff < 0, "Lewis", "Max")), size = 5) +
                theme_msc() +
                theme(
                        axis.text = element_text(size = 11),
                        strip.text = element_text(size = 10),
                        strip.background = element_rect(color = "grey50")
                ) +
                labs(
                        x = "Lap Number",
                        y = "Lead"
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(abs(x), " secs")}
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis" = "seagreen",
                                "Max" = "navy"
                        )
                )
)

plot_grid(
        plt_multi[[1]],
        plt_multi[[2]],
        align = "v",
        nrow = 2,
        rel_heights = c(3/4, 1/4)
)

```

## Round 8

```{r round8}

plt_multi <- list(
        laptimes_lm2021 %>% 
                filter(
                        Round == 8
                ) %>% 
                ggplot() +
                geom_line(aes(x = Lap, y = Laptime, group = DriverName, color = DriverName), size = 1) +
                geom_point(aes(x = Lap, y = Laptime, color = DriverName), size = 3) +
                theme_msc() +
                theme(
                        panel.grid.major.y = element_line(linewidth = 0.5, linetype = "dashed", color = "grey80"),
                        axis.text.x = element_blank(),
                        legend.position = "top"
                ) +
                labs(
                        title = "Lap Times During Round 8",
                        subtitle = "Styrian Grand Prix",
                        x = "",
                        y = "Lap",
                        color = ""
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(floor(x / 60000), ":", str_pad((x / 1000) %% 60, 2, pad = "0"))},
                        breaks = c(seq(0, 175000, 5000))
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis Hamilton" = "seagreen",
                                "Max Verstappen" = "navy"
                        )
                ),
        laptimes_lm2021_diffs %>% 
                filter(
                        Round == 8
                ) %>% 
                mutate(
                        RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
                ) %>% 
                ggplot() +
                geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
                geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
                #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
                #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
                geom_text(aes(x = 1, y = -MaxDiff, label = "Lewis"), color = "seagreen", hjust = 0, size = 5) +
                geom_text(aes(x = 1, y = MaxDiff, label = "Max"), color = "navy", hjust = 0, size = 5) +
                geom_text(aes(x = Lap, y = ifelse(Lap == RaceLaps, TimeDiff + ifelse(TimeDiff < 0, -3, 3), NA), label = TimeDiff, color = ifelse(TimeDiff < 0, "Lewis", "Max")), size = 5) +
                theme_msc() +
                theme(
                        axis.text = element_text(size = 11),
                        strip.text = element_text(size = 10),
                        strip.background = element_rect(color = "grey50")
                ) +
                labs(
                        x = "Lap Number",
                        y = "Lead"
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(abs(x), " secs")}
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis" = "seagreen",
                                "Max" = "navy"
                        )
                )
)

plot_grid(
        plt_multi[[1]],
        plt_multi[[2]],
        align = "v",
        nrow = 2,
        rel_heights = c(3/4, 1/4)
)

```

## Round 9

```{r round9}

plt_multi <- list(
        laptimes_lm2021 %>% 
                filter(
                        Round == 9
                ) %>% 
                ggplot() +
                geom_line(aes(x = Lap, y = Laptime, group = DriverName, color = DriverName), size = 1) +
                geom_point(aes(x = Lap, y = Laptime, color = DriverName), size = 3) +
                theme_msc() +
                theme(
                        panel.grid.major.y = element_line(linewidth = 0.5, linetype = "dashed", color = "grey80"),
                        axis.text.x = element_blank(),
                        legend.position = "top"
                ) +
                labs(
                        title = "Lap Times During Round 9",
                        subtitle = "Austrian Grand Prix",
                        x = "",
                        y = "Lap",
                        color = ""
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(floor(x / 60000), ":", str_pad((x / 1000) %% 60, 2, pad = "0"))},
                        breaks = c(seq(0, 175000, 5000))
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis Hamilton" = "seagreen",
                                "Max Verstappen" = "navy"
                        )
                ),
        laptimes_lm2021_diffs %>% 
                filter(
                        Round == 9
                ) %>% 
                mutate(
                        RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
                ) %>% 
                ggplot() +
                geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
                geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
                #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
                #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
                geom_text(aes(x = 1, y = -MaxDiff, label = "Lewis"), color = "seagreen", hjust = 0, size = 5) +
                geom_text(aes(x = 1, y = MaxDiff, label = "Max"), color = "navy", hjust = 0, size = 5) +
                geom_text(aes(x = Lap, y = ifelse(Lap == RaceLaps, TimeDiff + ifelse(TimeDiff < 0, -3, 3), NA), label = TimeDiff, color = ifelse(TimeDiff < 0, "Lewis", "Max")), size = 5) +
                theme_msc() +
                theme(
                        axis.text = element_text(size = 11),
                        strip.text = element_text(size = 10),
                        strip.background = element_rect(color = "grey50")
                ) +
                labs(
                        x = "Lap Number",
                        y = "Lead"
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(abs(x), " secs")}
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis" = "seagreen",
                                "Max" = "navy"
                        )
                )
)

plot_grid(
        plt_multi[[1]],
        plt_multi[[2]],
        align = "v",
        nrow = 2,
        rel_heights = c(3/4, 1/4)
)

```

## Round 10

TBC

## Round 11

```{r round11}

plt_multi <- list(
        laptimes_lm2021 %>% 
                filter(
                        Round == 11
                ) %>% 
                ggplot() +
                geom_line(aes(x = Lap, y = Laptime, group = DriverName, color = DriverName), size = 1) +
                geom_point(aes(x = Lap, y = Laptime, color = DriverName), size = 3) +
                theme_msc() +
                theme(
                        panel.grid.major.y = element_line(linewidth = 0.5, linetype = "dashed", color = "grey80"),
                        axis.text.x = element_blank(),
                        legend.position = "top"
                ) +
                labs(
                        title = "Lap Times During Round 11",
                        subtitle = "Hungarian Grand Prix",
                        x = "",
                        y = "Lap",
                        color = ""
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(floor(x / 60000), ":", str_pad((x / 1000) %% 60, 2, pad = "0"))},
                        breaks = c(seq(0, 175000, 5000))
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis Hamilton" = "seagreen",
                                "Max Verstappen" = "navy"
                        )
                ) +
                coord_cartesian(
                        ylim = c(75000, 150000)
                ),
        laptimes_lm2021_diffs %>% 
                filter(
                        Round == 11
                ) %>% 
                mutate(
                        RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
                ) %>% 
                ggplot() +
                geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
                geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
                #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
                #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
                geom_text(aes(x = 1, y = -MaxDiff, label = "Lewis"), color = "seagreen", hjust = 0, size = 5) +
                geom_text(aes(x = 1, y = MaxDiff, label = "Max"), color = "navy", hjust = 0, size = 5) +
                geom_text(aes(x = Lap, y = ifelse(Lap == RaceLaps, TimeDiff + ifelse(TimeDiff < 0, -3, 3), NA), label = TimeDiff, color = ifelse(TimeDiff < 0, "Lewis", "Max")), size = 5) +
                theme_msc() +
                theme(
                        axis.text = element_text(size = 11),
                        strip.text = element_text(size = 10),
                        strip.background = element_rect(color = "grey50")
                ) +
                labs(
                        x = "Lap Number",
                        y = "Lead"
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(abs(x), " secs")}
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis" = "seagreen",
                                "Max" = "navy"
                        )
                )
)

plot_grid(
        plt_multi[[1]],
        plt_multi[[2]],
        align = "v",
        nrow = 2,
        rel_heights = c(3/4, 1/4)
)

```

## Round 12

TBC

## Round 13

```{r round13}

plt_multi <- list(
        laptimes_lm2021 %>% 
                filter(
                        Round == 13
                ) %>% 
                ggplot() +
                geom_line(aes(x = Lap, y = Laptime, group = DriverName, color = DriverName), size = 1) +
                geom_point(aes(x = Lap, y = Laptime, color = DriverName), size = 3) +
                theme_msc() +
                theme(
                        panel.grid.major.y = element_line(linewidth = 0.5, linetype = "dashed", color = "grey80"),
                        axis.text.x = element_blank(),
                        legend.position = "top"
                ) +
                labs(
                        title = "Lap Times During Round 13",
                        subtitle = "Dutch Grand Prix",
                        x = "",
                        y = "Lap",
                        color = ""
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(floor(x / 60000), ":", str_pad((x / 1000) %% 60, 2, pad = "0"))},
                        breaks = c(seq(0, 175000, 5000))
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis Hamilton" = "seagreen",
                                "Max Verstappen" = "navy"
                        )
                ),
        laptimes_lm2021_diffs %>% 
                filter(
                        Round == 13
                ) %>% 
                mutate(
                        RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
                ) %>% 
                ggplot() +
                geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
                geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
                #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
                #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
                geom_text(aes(x = 1, y = -MaxDiff, label = "Lewis"), color = "seagreen", hjust = 0, size = 5) +
                geom_text(aes(x = 1, y = MaxDiff, label = "Max"), color = "navy", hjust = 0, size = 5) +
                geom_text(aes(x = Lap, y = ifelse(Lap == RaceLaps, TimeDiff + ifelse(TimeDiff < 0, -3, 3), NA), label = TimeDiff, color = ifelse(TimeDiff < 0, "Lewis", "Max")), size = 5) +
                theme_msc() +
                theme(
                        axis.text = element_text(size = 11),
                        strip.text = element_text(size = 10),
                        strip.background = element_rect(color = "grey50")
                ) +
                labs(
                        x = "Lap Number",
                        y = "Lead"
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(abs(x), " secs")}
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis" = "seagreen",
                                "Max" = "navy"
                        )
                )
)

plot_grid(
        plt_multi[[1]],
        plt_multi[[2]],
        align = "v",
        nrow = 2,
        rel_heights = c(3/4, 1/4)
)

```

## Round 14

```{r round14}

plt_multi <- list(
        laptimes_lm2021 %>% 
                filter(
                        Round == 14
                ) %>% 
                ggplot() +
                geom_line(aes(x = Lap, y = Laptime, group = DriverName, color = DriverName), size = 1) +
                geom_point(aes(x = Lap, y = Laptime, color = DriverName), size = 3) +
                theme_msc() +
                theme(
                        panel.grid.major.y = element_line(linewidth = 0.5, linetype = "dashed", color = "grey80"),
                        axis.text.x = element_blank(),
                        legend.position = "top"
                ) +
                labs(
                        title = "Lap Times During Round 14",
                        subtitle = "Italian Grand Prix",
                        x = "",
                        y = "Lap",
                        color = ""
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(floor(x / 60000), ":", str_pad((x / 1000) %% 60, 2, pad = "0"))},
                        breaks = c(seq(0, 175000, 5000))
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis Hamilton" = "seagreen",
                                "Max Verstappen" = "navy"
                        )
                ),
        laptimes_lm2021_diffs %>% 
                filter(
                        Round == 14
                ) %>% 
                mutate(
                        RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
                ) %>% 
                ggplot() +
                geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
                geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
                #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
                #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
                geom_text(aes(x = 1, y = -MaxDiff, label = "Lewis"), color = "seagreen", hjust = 0, size = 5) +
                geom_text(aes(x = 1, y = MaxDiff, label = "Max"), color = "navy", hjust = 0, size = 5) +
                #geom_text(aes(x = Lap, y = ifelse(Lap == RaceLaps, TimeDiff + ifelse(TimeDiff < 0, -3, 3), NA), label = TimeDiff, color = ifelse(TimeDiff < 0, "Lewis", "Max")), size = 5) +
                theme_msc() +
                theme(
                        axis.text = element_text(size = 11),
                        strip.text = element_text(size = 10),
                        strip.background = element_rect(color = "grey50")
                ) +
                labs(
                        x = "Lap Number",
                        y = "Lead"
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(abs(x), " secs")}
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis" = "seagreen",
                                "Max" = "navy"
                        )
                )
)

plot_grid(
        plt_multi[[1]],
        plt_multi[[2]],
        align = "v",
        nrow = 2,
        rel_heights = c(3/4, 1/4)
)

```

## Round 15

```{r round15}

plt_multi <- list(
        laptimes_lm2021 %>% 
                filter(
                        Round == 15
                ) %>% 
                ggplot() +
                geom_line(aes(x = Lap, y = Laptime, group = DriverName, color = DriverName), size = 1) +
                geom_point(aes(x = Lap, y = Laptime, color = DriverName), size = 3) +
                theme_msc() +
                theme(
                        panel.grid.major.y = element_line(linewidth = 0.5, linetype = "dashed", color = "grey80"),
                        axis.text.x = element_blank(),
                        legend.position = "top"
                ) +
                labs(
                        title = "Lap Times During Round 15",
                        subtitle = "Russian Grand Prix",
                        x = "",
                        y = "Lap",
                        color = ""
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(floor(x / 60000), ":", str_pad((x / 1000) %% 60, 2, pad = "0"))},
                        breaks = c(seq(0, 175000, 5000))
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis Hamilton" = "seagreen",
                                "Max Verstappen" = "navy"
                        )
                ),
        laptimes_lm2021_diffs %>% 
                filter(
                        Round == 15
                ) %>% 
                mutate(
                        RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
                ) %>% 
                ggplot() +
                geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
                geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
                #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
                #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
                geom_text(aes(x = 1, y = -MaxDiff, label = "Lewis"), color = "seagreen", hjust = 0, size = 5) +
                geom_text(aes(x = 1, y = MaxDiff, label = "Max"), color = "navy", hjust = 0, size = 5) +
                geom_text(aes(x = Lap, y = ifelse(Lap == RaceLaps, TimeDiff + ifelse(TimeDiff < 0, -3, 3), NA), label = TimeDiff, color = ifelse(TimeDiff < 0, "Lewis", "Max")), size = 5) +
                theme_msc() +
                theme(
                        axis.text = element_text(size = 11),
                        strip.text = element_text(size = 10),
                        strip.background = element_rect(color = "grey50")
                ) +
                labs(
                        x = "Lap Number",
                        y = "Lead"
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(abs(x), " secs")}
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis" = "seagreen",
                                "Max" = "navy"
                        )
                )
)

plot_grid(
        plt_multi[[1]],
        plt_multi[[2]],
        align = "v",
        nrow = 2,
        rel_heights = c(3/4, 1/4)
)

```

## Round 16

```{r round16}

plt_multi <- list(
        laptimes_lm2021 %>% 
                filter(
                        Round == 16
                ) %>% 
                ggplot() +
                geom_line(aes(x = Lap, y = Laptime, group = DriverName, color = DriverName), size = 1) +
                geom_point(aes(x = Lap, y = Laptime, color = DriverName), size = 3) +
                theme_msc() +
                theme(
                        panel.grid.major.y = element_line(linewidth = 0.5, linetype = "dashed", color = "grey80"),
                        axis.text.x = element_blank(),
                        legend.position = "top"
                ) +
                labs(
                        title = "Lap Times During Round 16",
                        subtitle = "Turkish Grand Prix",
                        x = "",
                        y = "Lap",
                        color = ""
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(floor(x / 60000), ":", str_pad((x / 1000) %% 60, 2, pad = "0"))},
                        breaks = c(seq(0, 175000, 5000))
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis Hamilton" = "seagreen",
                                "Max Verstappen" = "navy"
                        )
                ),
        laptimes_lm2021_diffs %>% 
                filter(
                        Round == 16
                ) %>% 
                mutate(
                        RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
                ) %>% 
                ggplot() +
                geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
                geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
                #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
                #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
                geom_text(aes(x = 1, y = -MaxDiff, label = "Lewis"), color = "seagreen", hjust = 0, size = 5) +
                geom_text(aes(x = 1, y = MaxDiff, label = "Max"), color = "navy", hjust = 0, size = 5) +
                geom_text(aes(x = Lap, y = ifelse(Lap == RaceLaps, TimeDiff + ifelse(TimeDiff < 0, -3, 3), NA), label = TimeDiff, color = ifelse(TimeDiff < 0, "Lewis", "Max")), size = 5) +
                theme_msc() +
                theme(
                        axis.text = element_text(size = 11),
                        strip.text = element_text(size = 10),
                        strip.background = element_rect(color = "grey50")
                ) +
                labs(
                        x = "Lap Number",
                        y = "Lead"
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(abs(x), " secs")}
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis" = "seagreen",
                                "Max" = "navy"
                        )
                )
)

plot_grid(
        plt_multi[[1]],
        plt_multi[[2]],
        align = "v",
        nrow = 2,
        rel_heights = c(3/4, 1/4)
)

```

## Round 17

```{r round17}

plt_multi <- list(
        laptimes_lm2021 %>% 
                filter(
                        Round == 17
                ) %>% 
                ggplot() +
                geom_line(aes(x = Lap, y = Laptime, group = DriverName, color = DriverName), size = 1) +
                geom_point(aes(x = Lap, y = Laptime, color = DriverName), size = 3) +
                theme_msc() +
                theme(
                        panel.grid.major.y = element_line(linewidth = 0.5, linetype = "dashed", color = "grey80"),
                        axis.text.x = element_blank(),
                        legend.position = "top"
                ) +
                labs(
                        title = "Lap Times During Round 17",
                        subtitle = "United States Grand Prix",
                        x = "",
                        y = "Lap",
                        color = ""
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(floor(x / 60000), ":", str_pad((x / 1000) %% 60, 2, pad = "0"))},
                        breaks = c(seq(0, 175000, 5000))
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis Hamilton" = "seagreen",
                                "Max Verstappen" = "navy"
                        )
                ),
        laptimes_lm2021_diffs %>% 
                filter(
                        Round == 17
                ) %>% 
                mutate(
                        RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
                ) %>% 
                ggplot() +
                geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
                geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
                #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
                #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
                geom_text(aes(x = 1, y = -MaxDiff, label = "Lewis"), color = "seagreen", hjust = 0, size = 5) +
                geom_text(aes(x = 1, y = MaxDiff, label = "Max"), color = "navy", hjust = 0, size = 5) +
                geom_text(aes(x = Lap, y = ifelse(Lap == RaceLaps, TimeDiff + ifelse(TimeDiff < 0, -3, 3), NA), label = TimeDiff, color = ifelse(TimeDiff < 0, "Lewis", "Max")), size = 5) +
                theme_msc() +
                theme(
                        axis.text = element_text(size = 11),
                        strip.text = element_text(size = 10),
                        strip.background = element_rect(color = "grey50")
                ) +
                labs(
                        x = "Lap Number",
                        y = "Lead"
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(abs(x), " secs")}
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis" = "seagreen",
                                "Max" = "navy"
                        )
                )
)

plot_grid(
        plt_multi[[1]],
        plt_multi[[2]],
        align = "v",
        nrow = 2,
        rel_heights = c(3/4, 1/4)
)

```

## Round 18

```{r round18}

plt_multi <- list(
        laptimes_lm2021 %>% 
                filter(
                        Round == 18
                ) %>% 
                ggplot() +
                geom_line(aes(x = Lap, y = Laptime, group = DriverName, color = DriverName), size = 1) +
                geom_point(aes(x = Lap, y = Laptime, color = DriverName), size = 3) +
                theme_msc() +
                theme(
                        panel.grid.major.y = element_line(linewidth = 0.5, linetype = "dashed", color = "grey80"),
                        axis.text.x = element_blank(),
                        legend.position = "top"
                ) +
                labs(
                        title = "Lap Times During Round 18",
                        subtitle = "Mexico City Grand Prix",
                        x = "",
                        y = "Lap",
                        color = ""
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(floor(x / 60000), ":", str_pad((x / 1000) %% 60, 2, pad = "0"))},
                        breaks = c(seq(0, 175000, 5000))
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis Hamilton" = "seagreen",
                                "Max Verstappen" = "navy"
                        )
                ),
        laptimes_lm2021_diffs %>% 
                filter(
                        Round == 18
                ) %>% 
                mutate(
                        RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
                ) %>% 
                ggplot() +
                geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
                geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
                #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
                #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
                geom_text(aes(x = 1, y = -MaxDiff, label = "Lewis"), color = "seagreen", hjust = 0, size = 5) +
                geom_text(aes(x = 1, y = MaxDiff, label = "Max"), color = "navy", hjust = 0, size = 5) +
                geom_text(aes(x = Lap, y = ifelse(Lap == RaceLaps, TimeDiff + ifelse(TimeDiff < 0, -3, 3), NA), label = TimeDiff, color = ifelse(TimeDiff < 0, "Lewis", "Max")), size = 5) +
                theme_msc() +
                theme(
                        axis.text = element_text(size = 11),
                        strip.text = element_text(size = 10),
                        strip.background = element_rect(color = "grey50")
                ) +
                labs(
                        x = "Lap Number",
                        y = "Lead"
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(abs(x), " secs")}
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis" = "seagreen",
                                "Max" = "navy"
                        )
                )
)

plot_grid(
        plt_multi[[1]],
        plt_multi[[2]],
        align = "v",
        nrow = 2,
        rel_heights = c(3/4, 1/4)
)

```

## Round 19

```{r round19}

plt_multi <- list(
        laptimes_lm2021 %>% 
                filter(
                        Round == 19
                ) %>% 
                ggplot() +
                geom_line(aes(x = Lap, y = Laptime, group = DriverName, color = DriverName), size = 1) +
                geom_point(aes(x = Lap, y = Laptime, color = DriverName), size = 3) +
                theme_msc() +
                theme(
                        panel.grid.major.y = element_line(linewidth = 0.5, linetype = "dashed", color = "grey80"),
                        axis.text.x = element_blank(),
                        legend.position = "top"
                ) +
                labs(
                        title = "Lap Times During Round 19",
                        subtitle = "Sao Paulo Grand Prix",
                        x = "",
                        y = "Lap",
                        color = ""
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(floor(x / 60000), ":", str_pad((x / 1000) %% 60, 2, pad = "0"))},
                        breaks = c(seq(0, 175000, 5000))
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis Hamilton" = "seagreen",
                                "Max Verstappen" = "navy"
                        )
                ),
        laptimes_lm2021_diffs %>% 
                filter(
                        Round == 19
                ) %>% 
                mutate(
                        RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
                ) %>% 
                ggplot() +
                geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
                geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
                #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
                #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
                geom_text(aes(x = 1, y = -MaxDiff, label = "Lewis"), color = "seagreen", hjust = 0, size = 5) +
                geom_text(aes(x = 1, y = MaxDiff, label = "Max"), color = "navy", hjust = 0, size = 5) +
                geom_text(aes(x = Lap, y = ifelse(Lap == RaceLaps, TimeDiff + ifelse(TimeDiff < 0, -3, 3), NA), label = TimeDiff, color = ifelse(TimeDiff < 0, "Lewis", "Max")), size = 5) +
                theme_msc() +
                theme(
                        axis.text = element_text(size = 11),
                        strip.text = element_text(size = 10),
                        strip.background = element_rect(color = "grey50")
                ) +
                labs(
                        x = "Lap Number",
                        y = "Lead"
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(abs(x), " secs")}
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis" = "seagreen",
                                "Max" = "navy"
                        )
                )
)

plot_grid(
        plt_multi[[1]],
        plt_multi[[2]],
        align = "v",
        nrow = 2,
        rel_heights = c(3/4, 1/4)
)

```

## Round 20

```{r round20}

plt_multi <- list(
        laptimes_lm2021 %>% 
                filter(
                        Round == 20
                ) %>% 
                ggplot() +
                geom_line(aes(x = Lap, y = Laptime, group = DriverName, color = DriverName), size = 1) +
                geom_point(aes(x = Lap, y = Laptime, color = DriverName), size = 3) +
                theme_msc() +
                theme(
                        panel.grid.major.y = element_line(linewidth = 0.5, linetype = "dashed", color = "grey80"),
                        axis.text.x = element_blank(),
                        legend.position = "top"
                ) +
                labs(
                        title = "Lap Times During Round 20",
                        subtitle = "Qatar Grand Prix",
                        x = "",
                        y = "Lap",
                        color = ""
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(floor(x / 60000), ":", str_pad((x / 1000) %% 60, 2, pad = "0"))},
                        breaks = c(seq(0, 175000, 5000))
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis Hamilton" = "seagreen",
                                "Max Verstappen" = "navy"
                        )
                ),
        laptimes_lm2021_diffs %>% 
                filter(
                        Round == 20
                ) %>% 
                mutate(
                        RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
                ) %>% 
                ggplot() +
                geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
                geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
                #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
                #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
                geom_text(aes(x = 1, y = -MaxDiff, label = "Lewis"), color = "seagreen", hjust = 0, size = 5) +
                geom_text(aes(x = 1, y = MaxDiff, label = "Max"), color = "navy", hjust = 0, size = 5) +
                geom_text(aes(x = Lap, y = ifelse(Lap == RaceLaps, TimeDiff + ifelse(TimeDiff < 0, -3, 3), NA), label = TimeDiff, color = ifelse(TimeDiff < 0, "Lewis", "Max")), size = 5) +
                theme_msc() +
                theme(
                        axis.text = element_text(size = 11),
                        strip.text = element_text(size = 10),
                        strip.background = element_rect(color = "grey50")
                ) +
                labs(
                        x = "Lap Number",
                        y = "Lead"
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(abs(x), " secs")}
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis" = "seagreen",
                                "Max" = "navy"
                        )
                )
)

plot_grid(
        plt_multi[[1]],
        plt_multi[[2]],
        align = "v",
        nrow = 2,
        rel_heights = c(3/4, 1/4)
)

```

## Round 21

```{r round21}

plt_multi <- list(
        laptimes_lm2021 %>% 
                filter(
                        Round == 21
                ) %>% 
                ggplot() +
                geom_line(aes(x = Lap, y = Laptime, group = DriverName, color = DriverName), size = 1) +
                geom_point(aes(x = Lap, y = Laptime, color = DriverName), size = 3) +
                theme_msc() +
                theme(
                        panel.grid.major.y = element_line(linewidth = 0.5, linetype = "dashed", color = "grey80"),
                        axis.text.x = element_blank(),
                        legend.position = "top"
                ) +
                labs(
                        title = "Lap Times During Round 21",
                        subtitle = "Saudi Arabian Grand Prix",
                        x = "",
                        y = "Lap",
                        color = ""
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(floor(x / 60000), ":", str_pad((x / 1000) %% 60, 2, pad = "0"))},
                        breaks = c(seq(0, 175000, 5000))
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis Hamilton" = "seagreen",
                                "Max Verstappen" = "navy"
                        )
                ) +
                coord_cartesian(
                        ylim = c(75000, 150000)
                ),
        laptimes_lm2021_diffs %>% 
                filter(
                        Round == 21
                ) %>% 
                mutate(
                        RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
                ) %>% 
                ggplot() +
                geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
                geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
                #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
                #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
                geom_text(aes(x = 1, y = -MaxDiff, label = "Lewis"), color = "seagreen", hjust = 0, size = 5) +
                geom_text(aes(x = 1, y = MaxDiff, label = "Max"), color = "navy", hjust = 0, size = 5) +
                geom_text(aes(x = Lap, y = ifelse(Lap == RaceLaps, TimeDiff + ifelse(TimeDiff < 0, -3, 3), NA), label = TimeDiff, color = ifelse(TimeDiff < 0, "Lewis", "Max")), size = 5) +
                theme_msc() +
                theme(
                        axis.text = element_text(size = 11),
                        strip.text = element_text(size = 10),
                        strip.background = element_rect(color = "grey50")
                ) +
                labs(
                        x = "Lap Number",
                        y = "Lead"
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(abs(x), " secs")}
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis" = "seagreen",
                                "Max" = "navy"
                        )
                )
)

plot_grid(
        plt_multi[[1]],
        plt_multi[[2]],
        align = "v",
        nrow = 2,
        rel_heights = c(3/4, 1/4)
)

```

## Round 22

```{r round22}

plt_multi <- list(
        laptimes_lm2021 %>% 
                filter(
                        Round == 22
                ) %>% 
                ggplot() +
                geom_line(aes(x = Lap, y = Laptime, group = DriverName, color = DriverName), size = 1) +
                geom_point(aes(x = Lap, y = Laptime, color = DriverName), size = 3) +
                theme_msc() +
                theme(
                        panel.grid.major.y = element_line(linewidth = 0.5, linetype = "dashed", color = "grey80"),
                        axis.text.x = element_blank(),
                        legend.position = "top"
                ) +
                labs(
                        title = "Lap Times During Round 22",
                        subtitle = "Abu Dhabi Grand Prix",
                        x = "",
                        y = "Lap",
                        color = ""
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(floor(x / 60000), ":", str_pad((x / 1000) %% 60, 2, pad = "0"))},
                        breaks = c(seq(0, 175000, 5000))
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis Hamilton" = "seagreen",
                                "Max Verstappen" = "navy"
                        )
                ),
        laptimes_lm2021_diffs %>% 
                filter(
                        Round == 22
                ) %>% 
                mutate(
                        RoundRace = paste(sep = " ", str_pad(Round, 2, pad = "0"), RaceName)
                ) %>% 
                ggplot() +
                geom_hline(aes(yintercept = 0), linetype = "dotted", color = "grey50") +
                geom_line(aes(x = Lap, y = TimeDiff), color = "black", linewidth = 0.8) +
                #geom_point(aes(x = Lap, y = MaxDiff), color = "navy") +
                #geom_point(aes(x = Lap, y = -MaxDiff), color = "seagreen") +
                geom_text(aes(x = 1, y = -MaxDiff, label = "Lewis"), color = "seagreen", hjust = 0, size = 5) +
                geom_text(aes(x = 1, y = MaxDiff, label = "Max"), color = "navy", hjust = 0, size = 5) +
                geom_text(aes(x = Lap, y = ifelse(Lap == RaceLaps, TimeDiff + ifelse(TimeDiff < 0, -3, 3), NA), label = TimeDiff, color = ifelse(TimeDiff < 0, "Lewis", "Max")), size = 5) +
                theme_msc() +
                theme(
                        axis.text = element_text(size = 11),
                        strip.text = element_text(size = 10),
                        strip.background = element_rect(color = "grey50")
                ) +
                labs(
                        x = "Lap Number",
                        y = "Lead"
                ) +
                scale_y_continuous(
                        labels = function(x) {paste0(abs(x), " secs")}
                ) +
                scale_color_manual(
                        values = c(
                                "Lewis" = "seagreen",
                                "Max" = "navy"
                        )
                )
)

plot_grid(
        plt_multi[[1]],
        plt_multi[[2]],
        align = "v",
        nrow = 2,
        rel_heights = c(3/4, 1/4)
)

```


